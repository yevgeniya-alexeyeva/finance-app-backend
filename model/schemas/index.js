const transactionSchema = require('./transaction');
const { userSchema } = require('./user');
const categorySchema = require('./category');

module.exports = { transactionSchema, userSchema, categorySchema };
